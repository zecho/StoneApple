{% extends 'layout.html.twig' %}

{% block content %}

    {{ html|raw }}

    {% for post in posts %}
        <article>
            <h2>{{ post.title }}</h2>
            <p>{{ post.body|raw }}</p>
        </article>
    {% endfor %}  

{% endblock %}
